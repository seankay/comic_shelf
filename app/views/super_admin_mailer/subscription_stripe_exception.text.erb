    Stripe Error for <%= @subscription.id %>
    Subscription Information

    Subscription ID: <%= @subscription.id %>
    Subscription Plan: <%= @subscription.plan.name %>
    Store ID: <%= @subscription.store.id %>

    Exception:
    <%= @error_msg %>

